name: compat-runtime
services:
  api:
    image: alpine:3.20
    command: ["sh", "-c", "sleep 60"]
    depends_on:
      - db
    ports:
      - "8080:80"
    volumes:
      - data:/data
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_PASSWORD: example
volumes:
  data: {}
